{
	"$schema": "https://turbo.build/schema.json",
	"globalEnv": ["CLOUDFLARE_API_TOKEN"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".next/**", "!.next/cache/**"]
		},
		"pages:build": {
			"dependsOn": ["build"],
			"outputs": [".vercel/**", ".output/**"]
		},
		"deploy": {
			"dependsOn": ["build", "pages:build", "check-types"],
			"outputs": []
		},
		"check-types": {
			"dependsOn": ["^check-types"]
		},
		"migrate:local": {
			"cache": false
		},
		"migrate:remote": {
			"cache": false
		},
		"test": {
			"dependsOn": ["migrate:local"],
			"outputs": []
		}
	}
}
